apiVersion: 1

providers:
  - name: "video-server"
    orgId: 1
    folder: "Video Server"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
